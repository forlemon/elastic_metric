metricbeat.modules:
- module: nginx
  metricsets: ["stubstatus"]
  period: 10s

  # Nginx hosts
  hosts: ["http://{{ host_ip }}"]

  # Path to server status. Default server-status
  #server_status_path: "server-status"